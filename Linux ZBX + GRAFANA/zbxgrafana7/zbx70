#!/bin/bash
# Script de Instalação Automática: Zabbix 7.0 LTS + Grafana 11.x
# Compatível com Ubuntu 24.04 LTS (Noble Numbat)

# Desativar prompts interativos
pro config set apt_news=false > /dev/null 2>&1
export NEEDRESTART_MODE=a
export DEBIAN_FRONTEND=noninteractive

apt update
apt install -y ruby screen figlet toilet cowsay lolcat boxes curl pv vim software-properties-common build-essential -y > /dev/null 2>&1

toilet -w 100 -f smmono9 "Zabbix 7.0" | lolcat
toilet -w 100 -f smmono9 "Grafana 11" | lolcat
toilet -w 82 -f mini "Iniciando..." | lolcat

cd /tmp
rm *deb* > /dev/null 2>&1

# Configura Locales
apt install -y locales > /dev/null 2>&1
locale-gen pt_BR.UTF-8
update-locale LANG=pt_BR.UTF-8

# Instalação do MySQL Server
apt install -y mysql-server mysql-client -y

# Instalação do Repositório Zabbix 7.0 LTS para Ubuntu 24.04
wget https://repo.zabbix.com/zabbix/7.0/ubuntu/pool/main/z/zabbix-release/zabbix-release_7.0-1+ubuntu24.04_all.deb
dpkg -i zabbix-release_7.0-1+ubuntu24.04_all.deb
apt update

# Instalar pacotes do Zabbix Server e Frontend Apache/PHP
apt install -y zabbix-server-mysql zabbix-frontend-php zabbix-apache-conf zabbix-sql-scripts zabbix-agent

# Configuração do Banco de Dados do Zabbix no MySQL
mysql -uroot -e "CREATE DATABASE zabbix CHARACTER SET utf8mb4 COLLATE utf8mb4_bin;"
mysql -uroot -e "CREATE USER 'zabbix'@'localhost' IDENTIFIED BY '';"
mysql -uroot -e "GRANT ALL PRIVILEGES ON zabbix.* TO 'zabbix'@'localhost';"
mysql -uroot -e "SET GLOBAL log_bin_trust_function_creators = 1;"

figlet -w 100 -f smmono9 "BANCO ZABBIX 7.0" | lolcat
# Importação do Schema do Zabbix 7.0
zcat /usr/share/zabbix-sql-scripts/mysql/server.sql.gz | pv --progress --size `gzip -l /usr/share/zabbix-sql-scripts/mysql/server.sql.gz | sed -n 2p | awk '{print $2}'` | mysql --default-character-set=utf8mb4 -uroot zabbix

mysql -uroot -e "SET GLOBAL log_bin_trust_function_creators = 0;"

# Configurar PHP Timezone para América/Sao_Paulo
apt install -y php-mysql
a2enconf zabbix-frontend-php
sed -i 's/# php_value date.timezone Europe\/Riga/php_value date.timezone America\/Sao_Paulo/g' /etc/apache2/conf-enabled/zabbix.conf

# Instalação do Grafana 11.x (Repositório Oficial)
apt install -y apt-transport-https gpg
mkdir -p /etc/apt/keyrings
wget -q -O - https://apt.grafana.com/gpg.key | gpg --dearmor | tee /etc/apt/keyrings/grafana.gpg > /dev/null
echo "deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main" | tee /etc/apt/sources.list.d/grafana.list
apt update
apt install -y grafana

# Iniciar e Habilitar os Serviços
systemctl daemon-reload
systemctl restart zabbix-server zabbix-agent apache2 grafana-server
systemctl enable zabbix-server zabbix-agent apache2 grafana-server

# Instalar Plugin do Zabbix no Grafana
grafana-cli plugins install alexanderzobnin-zabbix-app
systemctl restart grafana-server

# Configurar o Data Source do Zabbix no Grafana via API local
sleep 10
curl -X POST "http://admin:admin@127.0.0.1:3000/api/datasources" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "zabbix",
    "type": "alexanderzobnin-zabbix-datasource",
    "access": "proxy",
    "url": "http://localhost/zabbix/api_jsonrpc.php",
    "jsonData": {
      "username": "Admin",
      "password": "zabbix"
    },
    "secureJsonData": {},
    "isDefault": true
  }' > /dev/null 2>&1

ip=`ip a | grep -oP '(?<=inet |addr:)(?:\d+\.){3}\d+' | grep -v 127.0.0.1 | head -n 1`
clear
echo -e "\n\tZabbix: http://$ip/zabbix \tUsuario: Admin - Senha: zabbix \n\tGrafana: http://$ip:3000 \tUsuario: admin - Senha: admin" | boxes -d spring -a hc -p h8 | lolcat
cowsay -f tux "Instalacao concluida! No wizard do zabbix, clique em avancar em todas as etapas!" | lolcat
toilet -w 100 -f mini "FIM." | lolcat
